query cexAccountsList($input: [CexAccountAndJwt!]!) {
  cexAccount {
    list(input: $input) {
      accountId
      cexAccountId
      cexName
      name
      isJwtValid
      apiKey
      image
    }
  }
}
